{
  "type": "loop",
  "description": "Sequentially verify each feature for spatial envelope contribution; newly discovered features are appended to the queue",
  "config": {
    "over": "$.prepare_feature_queue.output.feature_names",
    "as": "feature",
    "do": "phase5_verify_feature",
    "append_from": "new_features",
    "accumulate": "verifications"
  }
}
