{
  "description": "Crops feature bounding boxes from each view's cropped image. Returns per-view data including feature crop paths, ready for phase4.",
  "timeout_seconds": 120,
  "input_schema": {
    "type": "object",
    "properties": {
      "views": { "type": "string", "description": "JSON array of {name, path} view objects" },
      "phase3_results": { "type": "string", "description": "JSON array of per-view feature arrays from map_phase3" },
      "output_dir": { "type": "string" }
    },
    "required": ["views", "phase3_results", "output_dir"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "phase4_input": { "type": "array", "items": { "type": "object" } },
      "all_features_json": { "type": "string" },
      "non_geometric_views": { "type": "array", "items": { "type": "string" } }
    }
  }
}
